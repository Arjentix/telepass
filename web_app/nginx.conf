events {}

http {
    server {
        listen 80;
        listen 443 ssl;

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
    types {
        application/javascript js;
        application/wasm wasm;
        text/css css;
        text/html html;
    }
}
